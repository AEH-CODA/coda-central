from setuptools import setup, find_packages

setup(
    name='v6_logistic_regression_py',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'pandas',
        'scikit-learn'
    ],
)
