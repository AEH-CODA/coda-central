// Mirrors --color-chart-* in src/styles/tokens.css. Canvas 2D contexts can't
// resolve CSS custom properties, so chart libraries need literal values —
// keep this in sync with tokens.css by hand.
export const chartColors = ['#3b82f6', '#0ea5e9', '#64748b', '#d97706', '#16a34a', '#7c3aed']
export const primaryChartColor = '#3b82f6'
